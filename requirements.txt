fastapi==0.115.12
uvicorn[standard]==0.34.3
sqlalchemy==2.0.41
psycopg2-binary==2.9.10
alembic==1.15.2
pydantic==2.11.3
python-dotenv==1.1.0
httpx==0.28.1
# Verifying the Pub/Sub push OIDC token. Only the auth library is needed: the
# service receives events by HTTP push and never publishes, so it carries no
# broker client. google-auth's OIDC verification uses its requests transport,
# so requests is a direct dependency here.
google-auth==2.57.1
requests==2.34.2
pytest==8.3.5
ruff==0.11.12
